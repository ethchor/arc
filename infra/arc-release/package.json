{
  "name": "@arc/release",
  "version": "0.0.0",
  "private": true,
  "description": "Structural checks for the arc-server Dockerfile + release/CI workflows. Full image build + sign runs in CI.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.5",
    "js-yaml": "^4.1.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
