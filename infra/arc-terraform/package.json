{
  "name": "@arc/terraform",
  "version": "0.0.0",
  "private": true,
  "description": "Terraform module deploying the arc Helm chart. Validates structure via vitest; full terraform fmt+validate runs in CI.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.0.0",
    "js-yaml": "^4.1.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
